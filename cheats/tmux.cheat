% tmux

# new tmux session
tmux new -s <name>

# list tmux sessions
tmux ls

# attach to a session
tmux a -t <name>

# new tmux session in different directory
tmux new -d -c <directory> -s <name>

# kill session
tmux kill-session -t <name>
